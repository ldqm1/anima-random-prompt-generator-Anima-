# Release Notes — Anima 随机提示词生成器 v3.3.5

> 版本区间：v2.1.0 → v3.3.5（19 个提交）
>
> 本次发布是从「纯命令行工具」到「开箱即用的桌面软件」的里程碑升级。

---

## 🎉 核心变化：桌面图形界面（GUI）

**v2.1.0 是纯 CLI 工具（需要 Python + 配置文件）;v3.3.5 提供单文件 exe 桌面软件,不熟悉编程也能直接用。**

| 版本 | 框架 | 形态 |
|---|---|---|
| v2.1.0 | CLI | 命令行 + YAML 配置 |
| v3.0.0 | tkinter | 桌面 GUI（中文 4 Tab） |
| v3.3.0 | **PySide6 (Qt)** | 原生流畅桌面 GUI（6 Tab） |

---

## 🚀 新增功能

### 1. 无 API 模式（v3.3.5）—— 无需 API Key 也能用
- 生成「系统提示词 + 用户提示词」**完整模板**,一键复制后粘贴到**任意网页端 LLM**(DeepSeek 网页等)
- LLM 按指令输出最终单行提示词,结果放在**等宽代码框**方便复制
- 生成要求改为**仅输出提示词、无需 JSON**(新 `system_prompt_plain.md` + 模板纯文本模式)

### 2. 完整配置可视化编辑（v3.1.0 → v3.3.4）
- 高级页自动加载 `generation_config.yaml` + `creative_anchors.yaml` 全部配置项
- **左侧 12 章节导航**：基础设置 / 抽样数量 / 子类配额 / 反趋同词配额 / 生成侧重点 / 多角色 / r18 控制 / API 参数 / 额外要求池 / 角色池 / 类别白名单 / 创意锚点池
- **全配置汉化**：所有字段显示中文名（最少 tag 数 / 出现概率 / 跳过概率…）
- **悬停任意配置项 0.4 秒弹出说明**（英文配置键 + 作用与效果,357 项 100% 覆盖）
- 配置项全部平铺直接可见（无折叠）,深色/浅色主题自适应

### 3. 多配置预设（v3.2.0）
- 保存多套完整配置（生成参数 + 创意锚点）,**新建/复制/重命名/删除/切换**
- 导出 .yaml / 导入他人预设,切换即生效（生成用当前激活预设）

### 4. 深色模式（v3.2.0）
- 底部「外观」下拉：**浅色 / 深色 / 跟随系统**,即时切换并记住

### 5. 可搜索配置文档（v3.3.5）
- 自动生成 508 项配置详解 HTML,顶部**搜索框实时过滤**
- 含快速开始 3 步、7 张界面截图、11 章节导航、常见调整效果示例
- GUI 底部「📖 配置文档」按钮一键打开

### 6. OpenAI 兼容 API（v3.2.1）
- 支持任意 OpenAI 兼容接口（DeepSeek / Moonshot / OpenRouter / 本地 vLLM 等）
- 模型可编辑下拉,接口地址示例说明

---

## 🎨 界面美化与交互反馈（v3.3.5）

- **QSS 全面重写**：圆角卡片、Tab 胶囊选中、输入框聚焦高亮、渐变进度条、代码框深色底、滚动条圆角、手型光标
- **Toast 气泡提示**：复制/保存/生成/切换预设/主题切换均弹出右下角气泡（成功绿/错误红/信息蓝）
- **按钮闪烁反馈**：复制→"✓ 已复制"、保存→"✓ 已保存"、生成→"✓ 已生成 N 条"

---

## ⚙️ 其他改进

- **随机种子互斥单选**：随机/固定二选一（QRadioButton）
- **评级感知资源加载**：切 r18 自动重建知识库预过滤
- **大批量生成**：断点续存（停止后重开不重复）+ 实时进度条 + 可取消
- **proxy 支持**：api_config 可配代理
- **性能**：Qt 原生 + 懒加载折叠,主窗口构建 0.2s,滚动流畅

---

## 📦 打包与交付

- **单文件 exe**：`AnimaPromptGenerator.exe`（约 209MB,PySide6 版）
- 内置知识库（38681 条 tag）、角色池、提示词模板、配置文档
- 首次启动约 30-40 秒（解压资源）,之后秒开
- API Key 可选"记住"保存到用户目录,绝不写入项目

---

## 🐛 主要修复

- 打包 exe 窗口不显示（PyInstaller 收集优化）
- 配置文档截图中文方框（offscreen 渲染 → 真实窗口渲染）
- Toast 气泡不消失（QTimer 可靠销毁）
- 深色模式单选按钮不可见（QSS 指示器补齐）
- 高级页长键名截断 / API 说明裁剪 / 窗口缩放塌缩 / 生成页空白
- 额外要求池显示异常 / 类别白名单无提示 / 创意锚点文本框过窄

---

## 📋 使用前提

- Windows 10/11（64 位）
- 有 DeepSeek 或其他 OpenAI 兼容 API Key（用「生成」页）,**或** 无 Key 用「无 API 模式」+ 网页 LLM

## 🔗 快速上手

1. 双击 `AnimaPromptGenerator.exe`
2. 有 Key →「API 设置」填 Key →「生成」页开始；无 Key →「无 API 模式」生成模板复制给网页 LLM
3. 想调参数 →「高级」页左侧章节,悬停看说明,改完「保存设置」
4. 不懂的 → 底部「📖 配置文档」搜索
